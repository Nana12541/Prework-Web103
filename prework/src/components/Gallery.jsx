import React from 'react';
import ViewCreator from '../pages/ViewCreator'
//We must import the information from the component to then use it.

const Gallery = () => {
    return (
        <>
            <ViewCreator /> 
        </> 
        //this is to render the information that is in the View Creator Component. 
    )
};

export default Gallery; //To export the function.